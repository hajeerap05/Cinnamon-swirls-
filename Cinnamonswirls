
Agent
<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>CinnamonSwirls - Premium Cakes & Desserts</title> <meta name="description" content="Homemade cakes, bento cakes, brownies, cupcakes & desserts in premium quality. Order now from CinnamonSwirls!"> <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700;800&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet"> <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"> <style> * { margin: 0; padding: 0; box-sizing: border-box; }
    :root {
        --chocolate-brown: #4A2C2A;
        --mocha: #6B4E31;
        --cream: #F5F5F0;
        --beige: #EDE4E0;
        --soft-gold: #D4AF37;
        --warm-beige: #F8F1E9;
        --dark-chocolate: #2D1B18;
        --shadow: 0 25px 50px rgba(74, 44, 42, 0.15);
        --shadow-hover: 0 35px 70px rgba(74, 44, 42, 0.25);
    }
    body {
        font-family: 'Inter', sans-serif;
        line-height: 1.6;
        color: var(--dark-chocolate);
        overflow-x: hidden;
        background: var(--cream);
    }
    h1, h2, h3, h4, h5 {
        font-family: 'Playfair Display', serif;
    }
    /* Header */
    header {
        position: fixed;
        top: 0;
        width: 100%;
        background: rgba(245, 245, 240, 0.98);
        backdrop-filter: blur(20px);
        z-index: 1000;
        padding: 1rem 0;
        border-bottom: 1px solid var(--beige);
        transition: all 0.3s ease;
    }
    nav {
        max-width: 1400px;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0 2rem;
    }
    .logo {
        font-family: 'Playfair Display', serif;
        font-size: 2rem;
        font-weight: 800;
        background: linear-gradient(135deg, var(--chocolate-brown), var(--mocha));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }
    .nav-links {
        display: flex;
        list-style: none;
        gap: 2.5rem;
    }
    .nav-links a {
        text-decoration: none;
        color: var(--dark-chocolate);
        font-weight: 500;
        transition: all 0.3s ease;
        position: relative;
    }
    .nav-links a::after {
        content: '';
        position: absolute;
        width: 0;
        height: 2px;
        bottom: -5px;
        left: 0;
        background: var(--soft-gold);
        transition: width 0.3s ease;
    }
    .nav-links a:hover::after {
        width: 100%;
    }
    .nav-links a:hover {
        color: var(--chocolate-brown);
    }
    .header-actions {
        display: flex;
        align-items: center;
        gap: 1rem;
    }
    .cart-icon {
        position: relative;
        font-size: 1.4rem;
        cursor: pointer;
        color: var(--chocolate-brown);
        padding: 0.8rem;
        border-radius: 50%;
        transition: all 0.3s ease;
    }
    .cart-icon:hover {
        background: var(--beige);
        transform: scale(1.1);
    }
    .cart-count {
        position: absolute;
        top: 0;
        right: 0;
        background: var(--soft-gold);
        color: var(--dark-chocolate);
        border-radius: 50%;
        width: 22px;
        height: 22px;
        font-size: 0.75rem;
        font-weight: 700;
        display: flex;
        align-items: center;
        justify-content: center;
    }
    /* Hero Section */
    .hero {
        min-height: 100vh;
        background: linear-gradient(135deg, 
            rgba(74, 44, 42, 0.7) 0%, 
            rgba(107, 78, 49, 0.5) 50%), 
            url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 800"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="%23D4AF37" opacity="0.1"/><circle cx="75" cy="75" r="0.5" fill="%23F5F5F0" opacity="0.05"/></pattern></defs><rect width="1200" height="800" fill="url(%23grain)"/><rect fill="%23F8F1E9" opacity="0.3" x="0" y="0" width="1200" height="800" rx="200"/></svg>');
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        position: relative;
        overflow: hidden;
    }
    .hero-content {
        max-width: 900px;
        padding: 0 2rem;
        transform: translateY(40px);
        opacity: 0;
        animation: heroSlideUp 1.2s ease forwards;
    }
    @keyframes heroSlideUp {
        to {
            transform: translateY(0);
            opacity: 1;
        }
    }
    .hero h1 {
        font-size: clamp(3rem, 6vw, 5rem);
        font-weight: 800;
        margin-bottom: 1.5rem;
        background: linear-gradient(135deg, #fff, var(--warm-beige));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        line-height: 1.1;
    }
    .hero-subtitle {
        font-size: 1.4rem;
        margin-bottom: 3rem;
        color: rgba(255,255,255,0.95);
        font-weight: 400;
    }
    .hero-buttons {
        display: flex;
        gap: 1.5rem;
        justify-content: center;
        flex-wrap: wrap;
    }
    .btn {
        padding: 1.2rem 2.5rem;
        border: none;
        border-radius: 50px;
        font-size: 1.1rem;
        font-weight: 600;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 0.8rem;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        cursor: pointer;
        font-family: 'Inter', sans-serif;
        position: relative;
        overflow: hidden;
    }
    .btn-primary {
        background: linear-gradient(135deg, var(--soft-gold), #E8C547);
        color: var(--dark-chocolate);
        box-shadow: var(--shadow);
    }
    .btn-primary:hover {
        transform: translateY(-8px) scale(1.05);
        box-shadow: var(--shadow-hover);
    }
    .btn-secondary {
        background: rgba(255,255,255,0.2);
        color: white;
        border: 2px solid rgba(255,255,255,0.3);
        backdrop-filter: blur(10px);
    }
    .btn-secondary:hover {
        background: rgba(255,255,255,0.3);
        border-color: rgba(255,255,255,0.5);
        transform: translateY(-5px);
    }
    /* Sections */
    .section {
        padding: 6rem 0;
        max-width: 1400px;
        margin: 0 auto;
        padding-left: 2rem;
        padding-right: 2rem;
    }
    .section-title {
        text-align: center;
        font-size: clamp(2.5rem, 5vw, 4rem);
        margin-bottom: 4rem;
        background: linear-gradient(135deg, var(--chocolate-brown), var(--mocha));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        position: relative;
    }
    .section-title::after {
        content: '';
        position: absolute;
        bottom: -10px;
        left: 50%;
        transform: translateX(-50%);
        width: 80px;
        height: 4px;
        background: linear-gradient(135deg, var(--soft-gold), #E8C547);
        border-radius: 2px;
    }
    /* Categories Filter */
    .categories-filter {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
        margin-bottom: 3rem;
    }
    .category-btn {
        padding: 0.8rem 1.8rem;
        background: var(--warm-beige);
        color: var(--dark-chocolate);
        border: 2px solid transparent;
        border-radius: 30px;
        cursor: pointer;
        font-weight: 500;
        transition: all 0.3s ease;
    }
    .category-btn.active,
    .category-btn:hover {
        background: var(--soft-gold);
        border-color: var(--chocolate-brown);
        transform: translateY(-2px);
    }
    /* Products Grid */
    .products-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
        gap: 2.5rem;
        margin-top: 2rem;
    }
    .product-card {
        background: white;
        border-radius: 25px;
        overflow: hidden;
        box-shadow: var(--shadow);
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
    }
    .product-card:hover {
        transform: translateY(-20px);
        box-shadow: var(--shadow-hover);
    }
    .product-image {
        height: 280px;
        background: linear-gradient(135deg, var(--beige), var(--warm-beige));
        position: relative;
        overflow: hidden;
        display: flex;
        align-items: center;
        justify-content: center;
    }
    .product-image i {
        font-size: 4.5rem;
        color: var(--chocolate-brown);
        opacity: 0.8;
        animation: float 3s ease-in-out infinite;
    }
    @keyframes float {
        0%, 100% { transform: translateY(0px) rotate(0deg); }
        50% { transform: translateY(-10px) rotate(5deg); }
    }
    .product-badge {
        position: absolute;
        top: 20px;
        right: 20px;
        background: var(--soft-gold);
        color: var(--dark-chocolate);
        padding: 0.5rem 1rem;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 700;
    }
    .product-info {
        padding: 2rem;
    }
    .product-name {
        font-family: 'Playfair Display', serif;
        font-size: 1.6rem;
        font-weight: 600;
        margin-bottom: 0.8rem;
        color: var(--dark-chocolate);
        line-height: 1.2;
    }
    .product-price {
        font-size: 1.8rem;
        font-weight: 800;
        color: var(--chocolate-brown);
        margin-bottom: 1.5rem;
    }
    .product-actions {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }
    .quantity-selector {
        display: flex;
        align-items: center;
        gap: 1rem;
        background: var(--warm-beige);
        padding: 0.8rem 1.2rem;
        border-radius: 25px;
        flex: 1;
        min-width: 140px;
    }
    .qty-btn {
        width: 42px;
        height: 42px;
        border: 2px solid var(--beige);
        background: white;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        font-size: 1.2rem;
        transition: all 0.3s ease;
        color: var(--chocolate-brown);
    }
    .qty-btn:hover {
        background: var(--beige);
        transform: scale(1.1);
    }
    .quantity {
        font-weight: 600;
        min-width: 25px;
        text-align: center;
        color: var(--dark-chocolate);
    }
    /* Custom Order Section */
    .custom-order {
        background: linear-gradient(135deg, var(--chocolate-brown), var(--mocha));
        color: white;
        text-align: center;
        position: relative;
        overflow: hidden;
    }
    .custom-order::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="dots" x="0" y="0" width="10" height="10" patternUnits="userSpaceOnUse"><circle cx="5" cy="5" r="1" fill="rgba(255,255,255,0.1)"/></pattern></defs><rect width="100" height="100" fill="url(%23dots)"/></svg>');
    }
    .custom-content {
        position: relative;
        z-index: 2;
    }
    .custom-icon {
        font-size: 5rem;
        color: var(--soft-gold);
        margin-bottom: 2rem;
        animation: pulse 2s infinite;
    }
    @keyframes pulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.1); }
    }
    .custom-form {
        max-width: 600px;
        margin: 0 auto;
        display: grid;
        gap: 1.5rem;
    }
    .form-group {
        position: relative;
    }
    .form-group input,
    .form-group textarea,
    .form-group select {
        width: 100%;
        padding: 1.2rem;
        background: rgba(255,255,255,0.1);
        border: 2px solid rgba(255,255,255,0.2);
        border-radius: 15px;
        color: white;
        font-size: 1rem;
        backdrop-filter: blur(10px);
        transition: all 0.3s ease;
    }
    .form-group input::placeholder,
    .form-group textarea::placeholder {
        color: rgba(255,255,255,0.7);
    }
    .form-group input:focus,
    .form-group textarea:focus,
    .form-group select:focus {
        outline: none;
        border-color: var(--soft-gold);
        background: rgba(255,255,255,0.2);
        box-shadow: 0 0 0 4px rgba(212, 175, 55, 0.2);
    }
    /* Cart Modal */
    .cart-modal {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0,0,0,0.6);
        display: none;
        align-items: flex-end;
        justify-content: center;
        z-index: 2000;
        padding: 2rem;
        backdrop-filter: blur(15px);
    }
    .cart-modal.active {
        display: flex;
    }
    .cart-content {
        background: white;
        border-radius: 25px 25px 0 0;
        width: 100%;
        max-width: 500px;
        max-height: 90vh;
        overflow-y: auto;
        transform: translateY(100%);
        transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    }
    .cart-modal.active .cart-content {
        transform: translateY(0);
    }
    .cart-header {
        padding: 2rem 2rem 1rem;
        border-bottom: 1px solid var(--beige);
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
    .cart-title {
        font-family: 'Playfair Display', serif;
        font-size: 1.8rem;
        color: var(--dark-chocolate);
    }
    .close-cart {
        background: none;
        border: none;
        font-size: 1.6rem;
        cursor: pointer;
        color: var(--chocolate-brown);
        padding: 0.5rem;
        border-radius: 50%;
        transition: background 0.3s ease;
    }
    .close-cart:hover {
        background: var(--beige);
    }
    .cart-item {
        display: flex;
        gap: 1rem;
        padding: 1.5rem 2rem;
        border-bottom: 1px solid var(--beige);
    }
    .cart-item-image {
        width: 70px;
        height: 70px;
        border-radius: 15px;
        background: var(--warm-beige);
        display: flex;
        align-items: center;
        justify
